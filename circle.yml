machine:
  services:
      - docker

dependencies:
  override:
      - docker build -t aneris/aneris:latest .

test:
  override:
      # run coveralls on the first go-around
      - docker run aneris/aneris:latest /bin/bash -c "python2 -m pytest /aneris/tests --cov=aneris --cov-report term-missing --runslow && coveralls" 
      - docker run aneris/aneris:latest python3 -m pytest /aneris/tests --cov=aneris --cov-report term-missing --runslow